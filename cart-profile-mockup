import React, { useState } from ‘react’;
import {
Home, Star, ShoppingBag, User, ChevronRight, Minus, Plus, Trash2,
Package, CreditCard, Bell, Moon, HelpCircle, LogOut, Clock,
MapPin, Percent, Shield, Globe, X
} from ‘lucide-react’;

const KavaHubCartProfile = () => {
const [activeScreen, setActiveScreen] = useState(‘cart’); // ‘cart’ or ‘profile’
const [cartItems, setCartItems] = useState([
{
id: 1,
name: ‘Sunset Serenity’,
category: ‘Kava Cocktail’,
price: 12.00,
quantity: 2,
image: ‘🥥’,
modifications: [‘Large’, ‘Extra Shot’]
},
{
id: 2,
name: ‘Island Dreams’,
category: ‘Kava Cocktail’,
price: 14.00,
quantity: 1,
image: ‘🌺’,
modifications: [‘Medium’]
}
]);

const [darkMode, setDarkMode] = useState(false);
const [notifications, setNotifications] = useState(true);

const updateQuantity = (id, change) => {
setCartItems(prev => prev.map(item => {
if (item.id === id) {
const newQty = Math.max(0, item.quantity + change);
return newQty === 0 ? null : { …item, quantity: newQty };
}
return item;
}).filter(Boolean));
};

const removeItem = (id) => {
setCartItems(prev => prev.filter(item => item.id !== id));
};

const subtotal = cartItems.reduce((acc, item) => acc + (item.price * item.quantity), 0);
const deliveryFee = 3.00;
const tax = subtotal * 0.08;
const total = subtotal + deliveryFee + tax;
const cartCount = cartItems.reduce((acc, item) => acc + item.quantity, 0);

// Cart Screen Component
const CartScreen = () => (
<div className="flex flex-col h-screen bg-background">
{/* Status Bar */}
<div className="flex justify-between items-center px-6 pt-3 pb-2 text-primary">
<span className="text-base font-semibold tracking-tight">9:41</span>
<div className="flex items-center gap-1.5">
<div className="w-4 h-3 flex gap-0.5">
<span className="w-1 h-2 bg-primary rounded-sm opacity-40"></span>
<span className="w-1 h-2.5 bg-primary rounded-sm opacity-60"></span>
<span className="w-1 h-3 bg-primary rounded-sm opacity-80"></span>
<span className="w-1 h-3 bg-primary rounded-sm"></span>
</div>
<span className="text-xs">5G</span>
<div className="w-6 h-3 border border-primary rounded-sm p-0.5">
<div className="w-full h-full bg-primary rounded-sm"></div>
</div>
</div>
</div>

```
  {/* Header */}
  <div className="px-6 pt-2 pb-4 flex items-center justify-between">
    <h1 className="text-3xl font-bold text-primary tracking-tight">Cart</h1>
    {cartItems.length > 0 && (
      <button className="text-accent text-sm font-medium">Clear All</button>
    )}
  </div>

  {/* Main Content */}
  <div className="flex-1 overflow-y-auto">
    {cartItems.length === 0 ? (
      <div className="flex flex-col items-center justify-center h-full px-6">
        <div className="w-20 h-20 bg-accent/10 rounded-full flex items-center justify-center mb-4">
          <ShoppingBag size={32} className="text-accent" />
        </div>
        <h3 className="text-xl font-semibold text-primary mb-2">Your cart is empty</h3>
        <p className="text-secondary text-center mb-6">Add some kava goodness to get started</p>
        <button 
          onClick={() => setActiveScreen('home')}
          className="px-6 py-3 bg-accent text-white rounded-xl font-semibold hover:scale-105 transition-transform"
        >
          Browse Menu
        </button>
      </div>
    ) : (
      <>
        {/* Cart Items */}
        <div className="px-6 mb-6">
          {cartItems.map((item, index) => (
            <div key={item.id} className="mb-4">
              <div className="bg-surface rounded-2xl p-4 shadow-soft">
                <div className="flex gap-4">
                  <div className="w-16 h-16 bg-accent/10 rounded-xl flex items-center justify-center text-2xl">
                    {item.image}
                  </div>
                  <div className="flex-1">
                    <div className="flex justify-between items-start mb-1">
                      <h4 className="font-semibold text-primary">{item.name}</h4>
                      <button 
                        onClick={() => removeItem(item.id)}
                        className="p-1 hover:bg-red-50 rounded-lg transition-colors"
                      >
                        <Trash2 size={16} className="text-red-500" />
                      </button>
                    </div>
                    <p className="text-secondary text-xs mb-1">{item.category}</p>
                    <div className="flex flex-wrap gap-1 mb-2">
                      {item.modifications.map((mod, i) => (
                        <span key={i} className="text-xs text-accent bg-accent/10 px-2 py-0.5 rounded-full">
                          {mod}
                        </span>
                      ))}
                    </div>
                    <div className="flex items-center justify-between">
                      <span className="font-bold text-primary">${(item.price * item.quantity).toFixed(2)}</span>
                      <div className="flex items-center gap-3 bg-surface-hover rounded-lg p-1">
                        <button 
                          onClick={() => updateQuantity(item.id, -1)}
                          className="w-7 h-7 bg-white rounded-md flex items-center justify-center hover:scale-110 transition-transform shadow-sm"
                        >
                          <Minus size={14} className="text-primary" />
                        </button>
                        <span className="font-semibold text-primary min-w-[20px] text-center">{item.quantity}</span>
                        <button 
                          onClick={() => updateQuantity(item.id, 1)}
                          className="w-7 h-7 bg-white rounded-md flex items-center justify-center hover:scale-110 transition-transform shadow-sm"
                        >
                          <Plus size={14} className="text-primary" />
                        </button>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          ))}
        </div>

        {/* Promo Code */}
        <div className="px-6 mb-6">
          <div className="bg-surface rounded-2xl p-4 shadow-soft flex items-center gap-3">
            <Percent size={20} className="text-terracotta" />
            <input 
              type="text" 
              placeholder="Add promo code" 
              className="flex-1 text-primary placeholder-gray-400 outline-none"
            />
            <button className="text-accent font-semibold text-sm">Apply</button>
          </div>
        </div>

        {/* Order Summary */}
        <div className="px-6 pb-32">
          <div className="bg-surface rounded-2xl p-5 shadow-soft">
            <h3 className="font-semibold text-primary mb-4">Order Summary</h3>
            <div className="space-y-3">
              <div className="flex justify-between text-sm">
                <span className="text-secondary">Subtotal</span>
                <span className="text-primary font-medium">${subtotal.toFixed(2)}</span>
              </div>
              <div className="flex justify-between text-sm">
                <span className="text-secondary">Delivery Fee</span>
                <span className="text-primary font-medium">${deliveryFee.toFixed(2)}</span>
              </div>
              <div className="flex justify-between text-sm">
                <span className="text-secondary">Tax</span>
                <span className="text-primary font-medium">${tax.toFixed(2)}</span>
              </div>
              <div className="pt-3 border-t border-gray-100">
                <div className="flex justify-between">
                  <span className="font-semibold text-primary">Total</span>
                  <span className="font-bold text-primary text-lg">${total.toFixed(2)}</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </>
    )}
  </div>

  {/* Sticky Checkout Button */}
  {cartItems.length > 0 && (
    <div className="absolute bottom-20 left-0 right-0 px-6 pb-4 bg-gradient-to-t from-background via-background">
      <button className="w-full bg-accent text-white py-4 rounded-2xl font-semibold text-base shadow-premium hover:scale-[1.02] transition-transform">
        Continue to Checkout
      </button>
    </div>
  )}
</div>
```

);

// Profile Screen Component
const ProfileScreen = () => (
<div className="flex flex-col h-screen bg-background">
{/* Status Bar */}
<div className="flex justify-between items-center px-6 pt-3 pb-2 text-primary">
<span className="text-base font-semibold tracking-tight">9:41</span>
<div className="flex items-center gap-1.5">
<div className="w-4 h-3 flex gap-0.5">
<span className="w-1 h-2 bg-primary rounded-sm opacity-40"></span>
<span className="w-1 h-2.5 bg-primary rounded-sm opacity-60"></span>
<span className="w-1 h-3 bg-primary rounded-sm opacity-80"></span>
<span className="w-1 h-3 bg-primary rounded-sm"></span>
</div>
<span className="text-xs">5G</span>
<div className="w-6 h-3 border border-primary rounded-sm p-0.5">
<div className="w-full h-full bg-primary rounded-sm"></div>
</div>
</div>
</div>

```
  {/* Header */}
  <div className="px-6 pt-2 pb-4">
    <h1 className="text-3xl font-bold text-primary tracking-tight">Profile</h1>
  </div>

  {/* Main Content */}
  <div className="flex-1 overflow-y-auto pb-20">
    {/* User Info Card */}
    <div className="px-6 mb-6">
      <div className="bg-gradient-to-br from-accent to-accent-dark rounded-2xl p-5 shadow-premium">
        <div className="flex items-center gap-4">
          <div className="w-16 h-16 bg-white/20 rounded-full flex items-center justify-center backdrop-blur">
            <span className="text-2xl">👤</span>
          </div>
          <div className="flex-1">
            <h3 className="text-white font-semibold text-lg">Alex Thompson</h3>
            <p className="text-white/80 text-sm">alex.thompson@email.com</p>
            <p className="text-white/70 text-xs mt-1">Member since Oct 2024</p>
          </div>
        </div>
      </div>
    </div>

    {/* Menu Sections */}
    <div className="px-6 space-y-4">
      {/* Orders Section */}
      <div className="bg-surface rounded-2xl shadow-soft overflow-hidden">
        <button className="w-full px-4 py-4 flex items-center justify-between hover:bg-surface-hover transition-colors">
          <div className="flex items-center gap-3">
            <Package size={20} className="text-accent" />
            <span className="font-medium text-primary">My Orders</span>
          </div>
          <ChevronRight size={18} className="text-gray-400" />
        </button>
        <div className="border-t border-gray-50"></div>
        <button className="w-full px-4 py-4 flex items-center justify-between hover:bg-surface-hover transition-colors">
          <div className="flex items-center gap-3">
            <Clock size={20} className="text-accent" />
            <span className="font-medium text-primary">Order History</span>
          </div>
          <ChevronRight size={18} className="text-gray-400" />
        </button>
      </div>

      {/* Payment Section */}
      <div className="bg-surface rounded-2xl shadow-soft overflow-hidden">
        <button className="w-full px-4 py-4 flex items-center justify-between hover:bg-surface-hover transition-colors">
          <div className="flex items-center gap-3">
            <CreditCard size={20} className="text-accent" />
            <span className="font-medium text-primary">Payment Methods</span>
          </div>
          <ChevronRight size={18} className="text-gray-400" />
        </button>
        <div className="border-t border-gray-50"></div>
        <button className="w-full px-4 py-4 flex items-center justify-between hover:bg-surface-hover transition-colors">
          <div className="flex items-center gap-3">
            <MapPin size={20} className="text-accent" />
            <span className="font-medium text-primary">Delivery Addresses</span>
          </div>
          <ChevronRight size={18} className="text-gray-400" />
        </button>
      </div>

      {/* Preferences Section */}
      <div className="bg-surface rounded-2xl shadow-soft overflow-hidden">
        <div className="px-4 py-4 flex items-center justify-between">
          <div className="flex items-center gap-3">
            <Bell size={20} className="text-accent" />
            <span className="font-medium text-primary">Notifications</span>
          </div>
          <button 
            onClick={() => setNotifications(!notifications)}
            className={`w-12 h-7 rounded-full transition-colors ${
              notifications ? 'bg-accent' : 'bg-gray-200'
            }`}
          >
            <div className={`w-5 h-5 bg-white rounded-full shadow-sm transition-transform ${
              notifications ? 'translate-x-6' : 'translate-x-1'
            }`}></div>
          </button>
        </div>
        <div className="border-t border-gray-50"></div>
        <div className="px-4 py-4 flex items-center justify-between">
          <div className="flex items-center gap-3">
            <Moon size={20} className="text-accent" />
            <span className="font-medium text-primary">Dark Mode</span>
          </div>
          <button 
            onClick={() => setDarkMode(!darkMode)}
            className={`w-12 h-7 rounded-full transition-colors ${
              darkMode ? 'bg-accent' : 'bg-gray-200'
            }`}
          >
            <div className={`w-5 h-5 bg-white rounded-full shadow-sm transition-transform ${
              darkMode ? 'translate-x-6' : 'translate-x-1'
            }`}></div>
          </button>
        </div>
        <div className="border-t border-gray-50"></div>
        <button className="w-full px-4 py-4 flex items-center justify-between hover:bg-surface-hover transition-colors">
          <div className="flex items-center gap-3">
            <Globe size={20} className="text-accent" />
            <span className="font-medium text-primary">Language</span>
          </div>
          <div className="flex items-center gap-2">
            <span className="text-secondary text-sm">English (US)</span>
            <ChevronRight size={18} className="text-gray-400" />
          </div>
        </button>
      </div>

      {/* Support Section */}
      <div className="bg-surface rounded-2xl shadow-soft overflow-hidden">
        <button className="w-full px-4 py-4 flex items-center justify-between hover:bg-surface-hover transition-colors">
          <div className="flex items-center gap-3">
            <HelpCircle size={20} className="text-accent" />
            <span className="font-medium text-primary">Help & Support</span>
          </div>
          <ChevronRight size={18} className="text-gray-400" />
        </button>
        <div className="border-t border-gray-50"></div>
        <button className="w-full px-4 py-4 flex items-center justify-between hover:bg-surface-hover transition-colors">
          <div className="flex items-center gap-3">
            <Shield size={20} className="text-accent" />
            <span className="font-medium text-primary">Privacy & Security</span>
          </div>
          <ChevronRight size={18} className="text-gray-400" />
        </button>
      </div>

      {/* Logout */}
      <button className="w-full bg-surface rounded-2xl shadow-soft px-4 py-4 flex items-center justify-center gap-3 hover:bg-red-50 transition-colors">
        <LogOut size={20} className="text-red-500" />
        <span className="font-medium text-red-500">Log Out</span>
      </button>
    </div>
  </div>
</div>
```

);

return (
<>
{activeScreen === ‘cart’ ? <CartScreen /> : <ProfileScreen />}

```
  {/* Bottom Tab Bar */}
  <div className="absolute bottom-0 left-0 right-0 bg-white/95 backdrop-blur-xl border-t border-gray-100">
    <div className="flex justify-around items-center py-2 px-2">
      <button className="flex flex-col items-center gap-1 py-2 px-3 rounded-lg">
        <Home size={24} className="text-gray-400" strokeWidth={2} />
        <span className="text-xs text-gray-400">Home</span>
      </button>
      
      <button className="flex flex-col items-center gap-1 py-2 px-3 rounded-lg">
        <Star size={24} className="text-gray-400" strokeWidth={2} />
        <span className="text-xs text-gray-400">Rewards</span>
      </button>
      
      <button 
        onClick={() => setActiveScreen('cart')}
        className="flex flex-col items-center gap-1 py-2 px-3 rounded-lg relative"
      >
        <ShoppingBag size={24} className={activeScreen === 'cart' ? "text-accent" : "text-gray-400"} strokeWidth={activeScreen === 'cart' ? 0 : 2} fill={activeScreen === 'cart' ? "currentColor" : "none"} />
        {cartCount > 0 && (
          <div className="absolute -top-0.5 -right-0.5 w-5 h-5 bg-terracotta rounded-full flex items-center justify-center">
            <span className="text-white text-xs font-bold">{cartCount}</span>
          </div>
        )}
        <span className={`text-xs ${activeScreen === 'cart' ? 'font-medium text-accent' : 'text-gray-400'}`}>Cart</span>
      </button>
      
      <button 
        onClick={() => setActiveScreen('profile')}
        className="flex flex-col items-center gap-1 py-2 px-3 rounded-lg"
      >
        <User size={24} className={activeScreen === 'profile' ? "text-accent" : "text-gray-400"} strokeWidth={activeScreen === 'profile' ? 0 : 2} fill={activeScreen === 'profile' ? "currentColor" : "none"} />
        <span className={`text-xs ${activeScreen === 'profile' ? 'font-medium text-accent' : 'text-gray-400'}`}>Profile</span>
      </button>
    </div>
  </div>

  <style jsx>{`
    .bg-background { background: #FAFAF8; }
    .bg-surface { background: #FFFFFF; }
    .bg-surface-hover { background: #F5F5F3; }
    
    .text-primary { color: #1C1C1E; }
    .text-secondary { color: #8E8E93; }
    
    .bg-accent { background: #6B7F47; }
    .bg-accent-dark { background: #5A6B3B; }
    .text-accent { color: #6B7F47; }
    
    .bg-terracotta { background: #B8654B; }
    .text-terracotta { color: #B8654B; }
    
    .bg-gradient-to-br {
      background: linear-gradient(135deg, #6B7F47 0%, #5A6B3B 100%);
    }
    
    .bg-gradient-to-t {
      background: linear-gradient(to top, #FAFAF8 70%, transparent);
    }
    
    .shadow-premium {
      box-shadow: 0 10px 40px rgba(0,0,0,0.08), 0 2px 10px rgba(0,0,0,0.04);
    }
    
    .shadow-soft {
      box-shadow: 0 4px 20px rgba(0,0,0,0.04), 0 1px 4px rgba(0,0,0,0.02);
    }
    
    .backdrop-blur {
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
    }
    
    .backdrop-blur-xl {
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
    }
    
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'SF Pro Text', 'Helvetica Neue', sans-serif;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }
  `}</style>
</>
```

);
};

export default KavaHubCartProfile;
